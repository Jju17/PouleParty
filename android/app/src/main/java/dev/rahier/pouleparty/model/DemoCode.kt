package dev.rahier.pouleparty.model

/**
 * Hidden code that opens the in-app demo mode for App Review.
 * Long-press the START button on Home and type the code to reveal
 * the gameplay screens without needing GPS or a real multiplayer
 * session.
 */
object DemoCode {
    const val VALUE = "appreview"
}
